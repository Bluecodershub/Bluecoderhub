/**
 * Barrel export for layout components.
 * Provides a single import path for all layout components.
 * 
 * Usage:
 *   import { ErrorBoundary } from './components/layout';
 */

export { default as ErrorBoundary } from './ErrorBoundary.jsx';